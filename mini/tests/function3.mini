program FUNC3:
    declare (f, b, a0) integer;
    declare a real;

    function v2 (x real, y real) real :
        return x * x + y * y;
    end function v2;

    function v3 (x real, y real, z real) real :
        return x * x + y * y + z * z;
    end function v3;

    output  v2(1.0, 2.0);
    output  v3(1.0, 2.0, 3.0);
    
    return;
end program FUNC3;
